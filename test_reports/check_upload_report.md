# WKF 检测 & 上传报告

**报告时间**：2026-08-07 23:31
**项目**：WKF 威科夫三层量化分析系统（`E:\workbuddy\MT50802\wkf`）

---

## 一、检测结果

### 1.1 静态检查

| 检查项 | 结果 |
|---|---|
| 全量编译（py_compile，48 个 .py 文件） | ✅ **48/48 通过** |
| 语法检查（cli.py / run.py / web/server.py / wkf/ 全部模块） | ✅ 无语法错误 |
| 类型/导入冒烟（GUI offscreen 实例化 + 分析管线） | ✅ 正常 |

### 1.2 自动化测试

| 测试套件 | 结果 | 说明 |
|---|---|---|
| 端到端测试 `tools/test_e2e.py` | ⚠️ **36/40** | 38 项代码相关测试全过；**2 项失败为外部依赖**（见下） |
| 品种切换测试 `tools/test_switch_symbol.py` | ✅ **8/8** | 切换刷新/48h 窗口/防竞态/异常提示 |
| 表格 UI 断言（对齐/图标/颜色/加粗/斑马线/兼容） | ✅ **8/8** | 本次优化功能完整 |

### 1.3 失败项处理

e2e 的 2 个失败项（「AI 诊断真实调用」「AI 诊断 JSON」）原因：
> **DeepSeek API Key（尾号 45c51b）已被服务端判定无效**——`401 Authentication Fails, Your api key: ****c51b is invalid`。

- 该失败为**外部凭据问题，非代码缺陷**：同一 key 直连 `api.deepseek.com/v1/models` 同样返回 401
- 代码层面无法修复，需用户到 DeepSeek 控制台重新生成 Key
- 已确认：无 Key 时系统可自动降级纯规则模式（设计内行为），不阻塞其他功能

---

## 二、GitHub 上传状态

| 项 | 值 |
|---|---|
| 仓库地址 | https://github.com/lij55030-netizen/Wyckoff-MT5-Workbuddy |
| 分支 | `main`（本地与远程完全同步 ✅） |
| 本次提交 | `8160789` — chore: 加固.gitignore防止敏感配置备份入库 |
| 关联提交 | `30638e9` — feat: K线明细表格前端UI展示优化（上一轮已推送） |
| 敏感信息排查 | ✅ `config/settings.json`（含 API Key）已忽略未入库；新增 `*.bak`/`config/*.bak*` 忽略规则；已提交文件中无密钥/令牌/配置文件 |
| 无关文件 | ✅ 已清理本地残留（settings.json.bak、临时验证截图），未提交 |

---

## 三、修改内容总结

### 功能模块 1：K 线明细表格 UI 优化（`wkf/gui/main_window.py`，提交 `30638e9`）

| 改动 | 原因与目的 |
|---|---|
| 数据标签页改为 `_KlineTableWidget`（状态行 + QTableWidget） | 纯文本表格无法实现列宽/对齐/图标/配色等 UI 要求；**仅渲染层改动，行情数据/指标计算/业务逻辑零修改** |
| 11 列固定列宽 + 数字右对齐/时间左对齐 + 统一小数位 | 消除排版错位、边缘参差 |
| 涨跌列：阳 ↑绿 / 阴 ↓红（移除文字） | 视觉直观 |
| 量列：≥1.2x 均值 🔺 / ≤0.8x 🔻 / 常态 ⚫ | 放量缩量一目了然（均值取近 20 根，仅展示计算） |
| RSI 列：>70 ⚠️ / 30-70 ●灰 / <30 🔵 + 数值加粗 | 超买超卖状态可视化 |
| Δ 列：正 ↑绿 / 负 ↓红 / 0 —灰 | 颜色跟随涨跌 |
| 收盘/VWAP/RSI 加粗高亮 + 表头加深底色 + 斑马线 | 核心数据聚焦、长列表可读性 |
| `toPlainText()` 兼容方法 | 保持外部读取语义，测试脚本零改动 |
| 新增/删除模块 | 新增 `_KlineTableWidget` 类；`_render_data_tab` 保留未删 |

### 功能模块 2：仓库安全加固（`.gitignore`，提交 `8160789`）

| 改动 | 原因与目的 |
|---|---|
| 新增 `*.bak`、`config/*.bak*` 忽略规则 | 防止含 API Key 的配置备份（settings.json.bak）被误提交 |
| 清理本地残留 | settings.json.bak（敏感）、临时验证截图（无关文件） |

### 对现有功能的影响
- **零业务影响**：分析管线、图表、飞书、CLI、web 页面均未改动
- 兼容性：所有外部调用 `_tab_data.toPlainText()` 的测试脚本无需改动即通过

---

## 四、注意事项 / 潜在风险

1. ⚠️ **DeepSeek API Key 已失效（外部问题）**：AI 诊断功能暂不可用（会降级纯规则模式）。请到 DeepSeek 控制台重新生成 Key，发我后立即更新并复测。
2. 表格成交量均值标注使用**近 20 根均值**（≥1.2x 放量 / ≤0.8x 缩量）——阈值为展示层本地计算，如需调整阈值可告知。
3. 图标（🔺⚫🔻⚠️🔵）依赖系统 emoji 字体，个别 Windows 环境字体缺失时可能显示为方块，属字体环境问题，不影响数据。
4. 仓库不含 dist/ 打包产物（.gitignore 已排除），如需分发 EXE 请本地 `build_exe.bat` 构建。
