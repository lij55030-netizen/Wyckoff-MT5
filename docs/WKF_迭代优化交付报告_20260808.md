# WKF 威科夫 2.0 交易分析智能体 — 迭代优化交付报告

**交付版本**：WKF 迭代优化（基于 V1.3.1）
**交付时间**：2026-08-08
**署名**：向美
**项目路径**：`E:\workbuddy\MT50802\wkf`
**仓库**：github.com/lij55030-netizen/Wyckoff-MT5-Workbuddy

---

## 一、迭代范围总览

| 优先级 | 模块 | 状态 |
|---|---|---|
| P0 | 架构重构：main_window 渐进式拆分 + tests 单测目录 + 风险提示四位置 | ✅ 完成 |
| P0 | 存量 GUI 故障（时间戳/卡顿/图表交互/历史优化） | ✅ 大部分已在 V1.3.0/V1.3.1 落地，本次回归确认无退化 |
| P1 | 数据源抽象基类 + yfinance 适配层（可选依赖） | ✅ 完成 |
| P2 | 轻量化信号回测模块（零依赖只读） | ✅ 完成 |

---

## 二、P0-1 架构重构

### 2.1 main_window.py 渐进式拆分
【改动点】将 1559 行主窗口拆分为「主窗口（仅全局初始化/页面调度/信号绑定）+ 6 个独立子控件模块」。

【涉及文件】
| 文件 | 职责 | 行数 |
|---|---|---|
| `wkf/gui/main_window.py` | 全局初始化、子控件组装、信号绑定、线程/定时器调度 | 961（原 1559） |
| `wkf/gui/widgets/top_bar.py` | 顶部控制组件（品种/周期/按钮/状态/北京时钟） | 79 |
| `wkf/gui/widgets/kline_table.py` | K线明细表格控件（含填充/纯文本回滚渲染） | 214 |
| `wkf/gui/widgets/snapshot_panel.py` | 快照面板（保存按钮/预览/生成时间） | 114 |
| `wkf/gui/widgets/diagnosis_panel.py` | 诊断面板（顶部生成时间 + 三层推理） | 39 |
| `wkf/gui/widgets/decision_panel.py` | 决策面板（富文本四段 + 概率测算纯函数） | 198 |
| `wkf/gui/widgets/history_panel.py` | 历史记录面板 | 62 |
| `wkf/gui/widgets/ai_chat.py` | AI 对话面板 | 110 |
| `wkf/gui/widgets/backtest_panel.py` | 回测标签页（P2） | 新增 |

【兼容性】MainWindow 保留全部对外属性名（`_sym_combo`/`_tab_data`/`_tab_decision`/`_history_list` 等）与兼容方法（`_render_*`/`_compute_probabilities`/`toPlainText`），旧测试零改动通过；`_KlineTableWidget`/`_AiChatWidget` 保留别名。

【验证方式】全部历史回归测试通过（见第六节）。

### 2.2 tests 单元测试目录
【改动点】新建 `tests/` 目录，44 项自动化单测（标准库 unittest，零新增依赖）。

【涉及文件】
| 文件 | 覆盖 |
|---|---|
| `tests/test_indicators.py`（16 项） | RSI / EMA / ATR / 布林带 / VWAP / Delta / VolumeProfile(POC/VA) / Footprint 失衡与堆叠 |
| `tests/test_wyckoff.py`（15 项） | 价值区域（VAH/VAL/VPOC/VWAP/价格位置）/ 背景判定（趋势/区间/吸筹派发/背景不明）/ 订单流验证（失衡/堆叠/衰竭）/ 三层集成 |
| `tests/test_datasource.py`（9 项） | 数据源工厂 / MT5 契约 / yfinance 适配（含重采样纯函数） |
| `tests/test_backtest.py`（4 项） | 存档读写 / 回测统计 / 文本渲染 |

【验证方式】`python -m unittest discover tests` → 44/44 OK。

### 2.3 订单流风险提示（四位置固定添加）
统一文案：**「订单流由 MT5 Tick 数据近似换算生成，并非交易所原始盘口订单流，仅用于威科夫结构定性研判，不建议作为高频短线交易依据。」**

【涉及文件】
1. `wkf/wyckoff/analyzer.py` — 文件头部 docstring ✅
2. `wkf/wyckoff/orderflow_verify.py` — 文件头部 docstring ✅
3. `wkf/gui/widgets/decision_panel.py` — GUI 决策面板「④ 订单流结构」区块顶部（灰色小字）✅；快照页订单流区同步 ✅
4. `tools/generate_xau_report.py` + `tools/generate_xau_30m_report.py` — HTML 分析报告底部免责区 ✅
5. `wkf/notify/feishu_notifier.py` — 飞书推送文案末尾附注 ✅

【验证方式】执行分析 → 决策面板/快照/HTML 报告/飞书消息均可见风险提示文案。

---

## 三、P0-2 存量 GUI 故障（回归确认）

以下功能已在 V1.3.0/V1.3.1 落地，本次迭代未破坏（回归测试全过）：

| 需求 | 落地版本 | 本次回归 |
|---|---|---|
| 东八区北京时间戳（历史/快照/诊断） | V1.3.1 | ✅ test_v131 |
| 卡顿根治（异步线程/画布清理/内存+磁盘缓存/断开旧订阅） | V1.3.1 | ✅ test_v131 / test_switch |
| 图表交互（滚轮缩放/拖拽平移/视图保持/换品种重置） | V1.3.1 | ✅ test_v131 |
| 周期扩容 11 项 + 品种独立记忆 + 默认 XAU/5m | V1.3.0 | ✅ test_v130 |
| 订单流汉化 + Δ 列修复 + 顶部北京时钟 | V1.3.0/V2.4 | ✅ test_v130/v24 |
| 概率>60% 提示音 + 飞书 66.5% 加粗 + 邮箱标签 | V1.3.0/V2.4 | ✅ test_v130/v24 |

**测试隔离修复**（本迭代新增）：
- `tools/test_v131_features.py`：缓存往返验证改用独立测试键 `TST!`，避免 60 根缓存污染 switch 测试（V1.3.1 磁盘缓存引入的跨测试耦合）。
- `tools/test_switch_symbol.py`：移除测试内文件删除（沙箱拦截），依赖 TST! 隔离方案。

---

## 四、P1 可选数据源拓展（yfinance）

【改动点】
1. `wkf/data/datasource.py`（新增）：统一 `DataSource` 抽象基类（fetch_bars/get_tick_size/has_ticks/available_symbols）+ `MT5Source`（适配既有实现，完整功能）+ `YfinanceSource`（BTC-USD/^GSPC/^NDX/^DJI，无 Tick）+ `get_data_source()` 工厂。
2. `wkf/config/settings.py`：`GeneralSettings.data_source`（默认 "mt5"）。
3. `wkf/orchestrator/runner.py`：`_fetch_bars_via_source()` — 取数统一走数据源抽象（mt5 模式行为与旧版完全一致）。
4. `wkf/gui/settings_dialogs.py`：「其他设置」新增「行情数据源」下拉（MT5 / yfinance）。
5. `wkf/gui/main_window.py`：yfinance 模式品种下拉自动切换为通用品种；启动弹窗提示「无 Tick 数据，订单流/足迹图/实时价格线自动隐藏」；订单流面板渲染层自动省略。
6. 打包不内置 yfinance：未安装时 `YfinanceSource._require()` 抛出带安装说明的异常；用户 `pip install yfinance` 后即可启用。

【验证方式】
- 单测 `tests/test_datasource.py` 9/9 通过；
- 实测 yfinance 拉取 `BTC-USD 15m` 成功（最新收盘 64917.45）；
- mt5 模式全量回归不变（e2e 40/40）。

---

## 五、P2 轻量化信号回测模块（零依赖只读）

【改动点】
1. `wkf/backtest/archive.py`（新增）：分析完成时把 bias/trigger/invalidation/price/prob 追加落盘 `output/history_archive.json`（仅保留最近 2000 条；只写不改）。
2. `wkf/backtest/statistics.py`（新增）：`compute_backtest()` — 信号触发总次数（按品种/周期/方向）、高概率信号命中率（多/空≥66.5% 占比近似）、价格验证区间分布（above/below/inside VA）。
3. `wkf/gui/widgets/backtest_panel.py`（新增）：GUI「📈 回测」标签页，表格展示统计 + 摘要文本 + 刷新按钮（只读，禁止模拟交易/资金曲线）。
4. `wkf/gui/main_window.py`：`_on_analysis_done` 完成分析 → 落盘存档 + 回测页自动刷新。

【验证方式】单测 `tests/test_backtest.py` 4/4；模拟分析完成 → 存档追加 + 回测统计正确（GC1!/5m/long 各 2 次、高概率 2 次）。

---

## 六、整体回归结果

| 测试 | 结果 |
|---|---|
| `python -m unittest discover tests`（新增单测） | ✅ 44/44 |
| `tools/test_e2e.py` | ✅ 40/40 |
| `tools/test_v131_features.py`（需求1-3 回归） | ✅ 全部通过 |
| `tools/test_v130_features.py`（V1.3.0 回归） | ✅ 全部通过 |
| `tools/test_v24_features.py`（V2.4 回归） | ✅ 全部通过 |
| `tools/test_switch_symbol.py`（品种切换） | ✅ 8/8 |
| `tools/test_crosshair_integration.py`（十字线+价格线） | ✅ 全部通过 |

**兼容性结论**：向下 100% 兼容旧版本配置（settings.json）、快照（output/snapshot_*）、历史分析记录、打包部署逻辑（cli.py/run.py/启动 bat 未改动入口）；程序保持只读行情分析定位，无任何自动交易执行逻辑。

---

## 七、使用说明

- 单测运行：`python -m unittest discover tests -v`（无新增依赖）
- 回测入口：面板「📈 回测」标签页（需先执行若干次「提交分析」积累存档）
- yfinance 模式：设置 → 其他设置 → 行情数据源 → yfinance（需手动 `pip install yfinance`）
- 风险提示：分析结果（GUI/HTML/飞书/源码头部）均固定展示订单流 Tick 近似换算提示

> ⚠ 以上分析仅供学习研究，不构成投资建议。
